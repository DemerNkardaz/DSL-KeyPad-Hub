export const repositoryOwner = 'DemerNkardaz';
export const targetRepository = 'DSL-KeyPad';
export const repositorySources = {
	github: `https://github.com/${repositoryOwner}/${targetRepository}`,
	jsdelivr: `https://cdn.jsdelivr.net/gh/${repositoryOwner}/${targetRepository}`,
	api_github: `https://api.github.com/repos/${repositoryOwner}/${targetRepository}`,
	api_jsdelivr: `https://data.jsdelivr.com/v1/package/gh/${repositoryOwner}/${targetRepository}`
}

